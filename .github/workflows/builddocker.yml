name: Docker Image CI

on:
  workflow_dispatch:
jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Azure Container Registry login and build
      uses: Azure/docker-login@v1
      with:
        # Container registry username
        username: ${{ secrets.acrlogin }}
        # Container registry password
        password: ${{ secrets.acrpassword }}
        # Container registry server url
        login-server: ${{ secrets.acrserver }}
    - run: |
       chmod +x ${GITHUB_WORKSPACE}/ContainerApp/demoApp/increment_version.sh
       NEW_VERSION=$(./ContainerApp/demoApp/increment_version.sh)
       echo "The new version is $NEW_VERSION"
       echo "the update version is ===============$NEW_VERSION"
       REV_TAG=$(git log -1 --pretty=format:%h)
       cd ${GITHUB_WORKSPACE}/ContainerApp/demoApp
       docker build . --file Dockerfile -t ${{ secrets.acrserver }}/srstest:v$REV_TAG
       docker push ${{ secrets.acrserver }}/srstest:v$REV_TAG
       
    # - name: Azure Container Registry Login
    #   uses: Azure/docker-login@v1
    #   with:
    #     # Container registry username
    #     username: ${{ secrets.acrlogin }}
    #     # Container registry password
    #     password: ${{ secrets.acrpassword }}
    #     # Container registry server url
    #     login-server: ${{ secrets.acrserver }}
    # - run: |
    #     docker push ${{ secrets.acrserver }}/srstest:${{ github.sha }}
