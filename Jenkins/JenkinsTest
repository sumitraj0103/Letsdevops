pipeline {
    agent any

    stages {
        stage('Checkout the Repo') {
            steps {
                echo 'Hello World'
                echo "Build My solution using Repo"
            }
        }
         stage('cloning the project from git'){
          steps{
              git branch: "main", url: "${params.giturl}"
          }
        }
    }
}

